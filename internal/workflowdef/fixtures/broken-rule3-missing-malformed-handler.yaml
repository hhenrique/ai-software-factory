workflow: broken-rule3-missing-malformed-handler
version: 1

roles: [planner]

steps:
  - id: plan
    type: agent
    role: planner
    output_schema: { verdict: [proceed, reject] }
    on:
      proceed: COMPLETED
      reject: FAILED
